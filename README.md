How to Run the script:

        To run the script you can use, `bash setup_project.sh` or './setup_project.sh`
After running it, you get prompted to enter Parent directory version and after entering it, and pressing enter the script gets initiated.
